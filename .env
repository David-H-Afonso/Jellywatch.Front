# Jellywatch Frontend — Environment Variables
# Copy to .env.local for local overrides (not committed).

# Default Jellyfin server URL pre-filled in the login form.
# Change this to match your server's address.
VITE_JELLYFIN_DEFAULT_URL=https://jellyfin.davidhormigafonso.work
